<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Project CRUD</title>
    <link rel="stylesheet" href="/public/styles/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <!-- Google Font for Netflix feel -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="bg-[#141414] text-white font-poppins min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-lg bg-[#1A1A1A] p-8 rounded-2xl shadow-lg border border-zinc-700">
      <!-- Title -->
      <h1 class="text-3xl font-bold mb-6 text-center text-[#E50914] tracking-tight">Project - CRUD</h1>

      <!-- Form -->
      <form action="/create" method="POST" class="flex flex-col gap-5">
        <input
          class="px-5 py-3 border border-zinc-700 rounded-lg bg-[#222222] placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-[#E50914] transition"
          type="text"
          name="name"
          placeholder="Full Name"
          required
        />
        <input
          class="px-5 py-3 border border-zinc-700 rounded-lg bg-[#222222] placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-[#E50914] transition"
          type="email"
          name="email"
          placeholder="Email Address"
          required
        />
        <input
          class="px-5 py-3 border border-zinc-700 rounded-lg bg-[#222222] placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-[#E50914] transition"
          type="text"
          name="imageURL"
          placeholder="Image URL"
        />

        <!-- Create Button -->
        <button
          class="w-full py-3 bg-[#E50914] text-white rounded-lg font-semibold shadow-md hover:shadow-lg hover:shadow-[#E50914]/60 hover:bg-red-700 hover:-translate-y-1 transform transition-all duration-200"
          type="submit"
        >
          ➕ Create User
        </button>
      </form>

      <!-- View All Users Button -->
      <div class="mt-6 text-center">
        <a href="/read" 
           class="inline-block w-full py-3 bg-[#333333] text-white rounded-lg font-semibold shadow-md hover:shadow-lg hover:shadow-[#E50914]/50 hover:bg-[#E50914] hover:-translate-y-1 transform transition-all duration-200">
          👀 View All Users
        </a>
      </div>
    </div>

  </body>
</html>
