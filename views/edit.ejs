<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit User | Netflix Style</title>
    <link rel="stylesheet" href="/public/styles/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="bg-[#141414] text-white font-poppins min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-md bg-[#1A1A1A] rounded-2xl shadow-lg p-8 border border-zinc-700">
      <!-- Navigation -->
      <div class="mb-8 text-right">
        <a href="/read" 
           class="inline-flex items-center gap-2 px-4 py-2 bg-[#333333] text-white text-sm font-medium rounded-lg shadow-md hover:shadow-lg hover:shadow-[#E50914]/50 hover:bg-[#E50914] hover:-translate-y-1 transform transition-all duration-200">
           👀 Read Users
        </a>
      </div>

      <!-- Title -->
      <h1 class="text-3xl font-bold tracking-tight mb-6 text-center text-[#E50914]">Edit User</h1>

      <!-- Form -->
      <form action="/update/<%= user._id %>" method="POST" class="space-y-4">
        <input
          class="w-full px-5 py-3 border border-zinc-700 rounded-lg bg-[#222222] placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-[#E50914] transition"
          type="text"
          name="name"
          placeholder="Full Name"
          value="<%= user.name %>"
          required
        />
        
        <input
          class="w-full px-5 py-3 border border-zinc-700 rounded-lg bg-[#222222] placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-[#E50914] transition"
          type="email"
          name="email"
          placeholder="Email Address"
          value="<%= user.email %>"
          required
        />
        
        <input
          class="w-full px-5 py-3 border border-zinc-700 rounded-lg bg-[#222222] placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-[#E50914] transition"
          type="text"
          name="imageURL"
          placeholder="Image URL"
          value="<%= user.imageURL %>"
        />
        
        <!-- Update Button -->
        <button
          type="submit"
          class="w-full py-3 bg-[#E50914] text-white rounded-lg font-semibold shadow-md hover:shadow-lg hover:shadow-[#E50914]/60 hover:bg-red-700 hover:-translate-y-1 transform transition-all duration-200"
        >
           ✏️ Update User
        </button>
      </form>
    </div>

  </body>
</html>
